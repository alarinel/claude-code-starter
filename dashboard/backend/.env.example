# Dashboard backend configuration

# Server port (default: 5001)
PORT=5001

# SQLite database path (default: ./dashboard.db)
# For MySQL/PostgreSQL, update knexfile and install the appropriate driver
DB_PATH=./dashboard.db
